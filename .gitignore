# Environment files — NEVER commit real secrets/config
.env
*.env
!.env.example

# Python
venv/
__pycache__/
*.pyc
backend/artifacts/*
# ...except the real-data evaluation proof (metrics + plots) — these are
# committed on purpose so they render in the GitHub README.
!backend/artifacts/real_data_eval/
!backend/artifacts/real_data_eval/**

# Real dataset (large file, download separately — see backend/data/README.md)
backend/data/*.csv

# Node / React
frontend/node_modules/
frontend/build/

# OS
.DS_Store
Thumbs.db
